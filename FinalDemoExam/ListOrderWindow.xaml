<Window x:Class="FinalDemoExam.ListOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Список заказов" Height="740" Width="1200" MinHeight="620" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF"
        Icon="/Image/Icon.ico">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="1" Padding="12" Margin="0,0,0,12">
            <DockPanel>
                <TextBlock Text="Список заказов" FontSize="22" FontWeight="Bold" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <TextBlock Text="Пользователь: " FontWeight="Bold"/>
            <TextBlock x:Name="txtUserFullName" Text="" FontWeight="Bold" Foreground="#7FFF00"/>
            <Button x:Name="btnBack" Content="Назад к товарам" Background="#00FA9A" Click="btnBack_Click" Margin="20,0,0,0"/>
        </StackPanel>
            </DockPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <Button x:Name="btnAdd" Content="Добавить заказ" Background="#00FA9A" Click="btnAdd_Click" Visibility="Collapsed"/>
        </StackPanel>

        <ListView x:Name="lvOrders" Grid.Row="2" Margin="0,8,0,0" MouseDoubleClick="lvOrders_MouseDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="1" Padding="5" Margin="2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="2" Padding="10">
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding id}" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding OrderStatuses.name}" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding PickupPoints.address}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding order_date, StringFormat={}{0:dd.MM.yyyy}}" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="2" Margin="2" Padding="5" VerticalAlignment="Stretch">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Margin="0,5,0,0">
                                        <TextBlock.Text>
                                            <Binding Path="delivery_date" StringFormat="{}{0:dd.MM.yyyy}"/>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>