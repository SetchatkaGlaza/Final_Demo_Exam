<Window x:Class="FinalDemoExam.ListOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Список заказов" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF"
        Icon="/Image/Icon.ico">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <TextBlock Text="Пользователь: " FontWeight="Bold"/>
            <TextBlock x:Name="txtUserFullName" Text="" FontWeight="Bold" Foreground="#7FFF00"/>
            <Button x:Name="btnBack" Content="Назад к товарам" Background="#00FA9A" Click="btnBack_Click" Margin="20,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <Button x:Name="btnAdd" Content="Добавить заказ" Background="#00FA9A" Click="btnAdd_Click" Visibility="Collapsed"/>
        </StackPanel>

        <ListView x:Name="lvOrders" Grid.Row="2" Margin="0,10,0,0" MouseDoubleClick="lvOrders_MouseDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="10,5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding id}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <ItemsControl ItemsSource="{Binding OrderItems}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock>
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} - {1} шт.">
                                                        <Binding Path="Products.name"/>
                                                        <Binding Path="quantity"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="{Binding order_date, StringFormat={}{0:dd.MM.yyyy}}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{Binding delivery_date, StringFormat={}{0:dd.MM.yyyy}}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="4" Text="{Binding PickupPoints.address}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="5" Text="{Binding Users.full_name}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="6" Text="{Binding pickup_code}" VerticalAlignment="Center" FontWeight="Bold"/>

                            <Border Grid.Column="7" Padding="5" CornerRadius="5" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding OrderStatuses.name}" FontWeight="Bold" Foreground="White"/>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Green"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding OrderStatuses.name}" Value="Новый">
                                                <Setter Property="Background" Value="Orange"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>